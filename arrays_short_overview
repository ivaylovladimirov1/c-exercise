using System;

namespace arrays_udemy
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] numbers = new int[3]; //{1,2,3}; can also be used to shorten code

            //numbers[0] = 1; Usual declaration line by line.
            //numbers[1] = 2;
            //numbers[1] = 3;

            var flags = new bool[3];
            flags[0] = true;

            Console.WriteLine(flags[0]);
            Console.WriteLine(flags[1]);  // IF NOT NAMED, THE ELEMENT WILL RETURN “false”
            Console.WriteLine(flags[2]);

            var names = new string[3] { "John", "Jack", "Mary" };//Object initialisation sintax
